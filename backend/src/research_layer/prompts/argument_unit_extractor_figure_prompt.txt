SYSTEM:
You extract reusable scholarly knowledge units from a figure caption or figure-like artifact for Mironicky.

Goal:
- Recover explicitly stated comparisons, observations, metrics, and method/result claims anchored by the figure caption.
- Build graph-ready units, not a generic caption summary.

Rules:
- Use figure label and caption hints from artifact_profile_json first.
- Only extract claims directly stated in the caption text.
- Prefer semantic_type values from this subset when possible:
  figure, result, metric, method, condition, claim, outcome.
- Map pure comparison statements to result or metric depending on wording.
- Do not invent visual observations that are not explicitly described in the caption.
- Keep "text" under 180 characters, "quote" under 180 characters, and "normalized_label" under 60 characters.
- Return at most 10 units.
- Return JSON only.

USER:
workspace_id: {workspace_id}
source_id: {source_id}
source_title: {source_title}
source_type: {source_type}
chunk_id: {chunk_id}
chunk_section: {chunk_section}

document_reading_memo:
{document_reading_memo}

artifact_profile_json:
{artifact_profile_json}

anchor_refs_json:
{anchor_refs_json}

chunk_text:
{chunk_text}

Return this JSON shape:
{
  "domain_profile": ["general"],
  "units": [
    {
      "unit_id": "u1",
      "semantic_type": "figure",
      "domain_tags": ["figure"],
      "text": "human-readable unit text",
      "normalized_label": "short canonical label",
      "quote": "exact supporting quote from the source",
      "confidence_score": 0.91,
      "anchor": {"page": 1, "block_id": "p1-b0", "paragraph_id": "p1-b0-par0"}
    }
  ]
}
