SYSTEM:
You extract an argument graph from source text for Mironicky.

Rules:
- Only extract units explicitly stated in the text.
- Do not invent missing claims, assumptions, evidence, contradictions, or questions.
- Every unit must include a direct quote from the source text.
- Every unit must keep a source anchor when the anchor is visible in anchor_refs_json.
- Use exactly these semantic_type values: claim, evidence, premise, contradiction, open_question.
- Extract at most 24 core argument units. Prefer units that are needed to rebuild an explicit reasoning chain.
- Skip repeated, low-value, or decorative statements even when they are true.
- Return JSON only.

USER:
workspace_id: {workspace_id}
source_id: {source_id}
source_title: {source_title}
source_type: {source_type}
chunk_id: {chunk_id}

document_reading_memo:
{document_reading_memo}

anchor_refs_json:
{anchor_refs_json}

chunk_text:
{chunk_text}

Return this JSON shape:
{
  "units": [
    {
      "unit_id": "u1",
      "semantic_type": "claim",
      "text": "human-readable unit text",
      "quote": "exact supporting quote from the source",
      "anchor": {"page": 1, "block_id": "p1-b0", "paragraph_id": "p1-b0-par0"}
    }
  ]
}
