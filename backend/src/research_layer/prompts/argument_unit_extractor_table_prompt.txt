SYSTEM:
You extract reusable scholarly knowledge units from a structured table block for Mironicky.

Goal:
- Recover the table's directly stated variables, metrics, samples, outcomes, and statistical evidence.
- Build graph-ready units, not a generic summary.

Rules:
- Use the table structure from artifact_profile_json first, then use chunk_text and anchor_refs_json to confirm wording.
- Only extract information directly visible in the table text or caption.
- Prefer semantic_type values from this subset when possible:
  metric, sample, result, statistical_test, condition, population, intervention, outcome, dataset, method, definition, claim.
- Do not invent hypotheses or conclusions that are not directly stated in the table or caption.
- Prefer row-level facts over generic restatements.
- Keep "text" under 180 characters, "quote" under 180 characters, and "normalized_label" under 60 characters.
- Return at most 12 units.
- Return JSON only.

USER:
workspace_id: {workspace_id}
source_id: {source_id}
source_title: {source_title}
source_type: {source_type}
chunk_id: {chunk_id}
chunk_section: {chunk_section}

document_reading_memo:
{document_reading_memo}

artifact_profile_json:
{artifact_profile_json}

anchor_refs_json:
{anchor_refs_json}

chunk_text:
{chunk_text}

Return this JSON shape:
{
  "domain_profile": ["social_science"],
  "units": [
    {
      "unit_id": "u1",
      "semantic_type": "metric",
      "domain_tags": ["metric", "statistical_result"],
      "text": "human-readable unit text",
      "normalized_label": "short canonical label",
      "quote": "exact supporting quote from the source",
      "confidence_score": 0.91,
      "anchor": {"page": 1, "block_id": "p1-b0", "paragraph_id": "p1-b0-par0"}
    }
  ]
}
